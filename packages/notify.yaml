notify:
  - platform: slack
    name: slack
    api_key: !secret slack_api_key
    default_channel: '#home-automation'
  - platform: kodi
    name: kodi
    host: taranis
  - platform: group
    name: default_group
    services:
      - service: slack
      - service: kodi
      - service: notify

mycroft:
  host: !secret mycroft_ip